// Desktop re-exports the shared ClipboardPanel which already supports
// desktop-specific props (autoCapture, togglePin) via optional parameters.
export { ClipboardPanel } from "@ui/components/ClipboardPanel";
