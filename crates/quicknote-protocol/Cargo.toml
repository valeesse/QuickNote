[package]
name = "quicknote-protocol"
version = "0.1.0"
edition = "2021"

[features]
sqlx = ["dep:sqlx"]

[dependencies]
serde = { version = "1", features = ["derive"] }
sqlx = { version = "0.8", optional = true, default-features = false, features = ["macros", "postgres"] }
